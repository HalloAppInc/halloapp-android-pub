
name: Master Push

on:
  push:
    branches: [ master, ci-test ]

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
    - uses: actions/checkout@v2

    - name: Set up JDK 1.8
      uses: actions/setup-java@v1
      with:
        java-version: 1.8

    # GitHub servers currently come with only version 21 installed. See: https://github.com/actions/virtual-environments/issues/578#issuecomment-605886387
    - name: Install NDK
      run: echo "y" | sudo /usr/local/lib/android/tools/bin/sdkmanager --install "ndk;20.0.5594570"
      
    - name: Assemble debug apk
      run: ./gradlew assembleDebug
