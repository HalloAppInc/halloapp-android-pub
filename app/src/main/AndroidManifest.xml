<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.halloapp">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <uses-sdk tools:overrideLibrary="androidx.security"/>

    <application
        android:name=".HalloApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="video/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="video/*" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.RegistrationVerificationActivity"
            android:label=""
            android:windowSoftInputMode="stateAlwaysVisible" />

        <activity
            android:name=".ui.RegistrationRequestActivity"
            android:label=""/>

        <activity android:name=".ui.InitialSyncActivity"
            android:label=""/>

        <activity android:name=".ui.chat.ChatActivity"
            android:parentActivityName=".ui.MainActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:label=""/>

        <activity
            android:name=".ui.ContentComposerActivity"
            android:label="@string/new_post"
            android:theme="@style/AppTheme.PostComposer"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.CommentsActivity"
            android:label="@string/comments"/>

        <activity
            android:name=".ui.SettingsActivity"
            android:label="@string/settings"/>

        <activity
            android:name=".ui.VideoPlaybackActivity"
            android:theme="@style/AppTheme.MediaPlayer"/>

        <activity
            android:name=".ui.mediapicker.MediaPickerActivity"
            android:label="@string/gallery"/>

        <activity
            android:name=".ui.PostDetailsActivity"
            android:label=""/>

        <activity android:name=".ui.AppExpirationActivity"
            android:label="" />

        <activity android:name=".ui.avatar.AvatarPreviewActivity"
            android:label="" />

        <receiver
            android:name=".registration.SmsVerificationReceiver"
            android:exported="true"
            android:permission="com.google.android.gms.auth.api.phone.permission.SEND">
            <intent-filter>
                <action android:name="com.google.android.gms.auth.api.phone.SMS_RETRIEVED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name = ".Notifications$DeleteNotificationReceiver"
            android:enabled = "true"
            android:exported = "true" >
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.halloapp.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <service
            android:name=".PushMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="false" />

    </application>

</manifest>